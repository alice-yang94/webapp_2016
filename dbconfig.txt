Database Configuration:

create table users (username varchar(20), password varchar(20), salt varchar(20));
create table completedGames (id SERIAL, username varchar(20), dateCompleted date, score integer);
create table currentGame (username varchar(20), level integer, score integer);
